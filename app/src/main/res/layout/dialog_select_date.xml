<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:background="@drawable/bg_bottom_sheet"
    android:padding="24dp">

    <TextView
        android:id="@+id/tvSelectDate"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Select Date"
        android:textColor="@android:color/black"
        android:textSize="20sp"
        android:textStyle="bold"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent" />

    <ImageButton
        android:id="@+id/btnClose"
        android:layout_width="48dp"
        android:layout_height="48dp"
        android:background="?attr/selectableItemBackgroundBorderless"
        android:src="@drawable/ic_close"
        app:layout_constraintBottom_toBottomOf="@id/tvSelectDate"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintTop_toTopOf="@id/tvSelectDate"
        app:tint="@android:color/black" />

    <LinearLayout
        android:id="@+id/layoutMonth"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="32dp"
        android:background="@drawable/bg_outline_gray"
        android:gravity="center"
        android:orientation="horizontal"
        android:padding="12dp"
        app:layout_constraintTop_toBottomOf="@id/tvSelectDate">

        <ImageView
            android:id="@+id/btnPrevMonth"
            android:layout_width="24dp"
            android:layout_height="24dp"
            android:src="@drawable/ic_back"
            android:clickable="true"
            android:focusable="true"
            app:tint="@android:color/black" />

        <TextView
            android:id="@+id/tvMonthYear"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:gravity="center"
            android:text="August 2025"
            android:textColor="@android:color/black"
            android:textSize="16sp"
            android:textStyle="bold" />

        <ImageView
            android:id="@+id/btnNextMonth"
            android:layout_width="24dp"
            android:layout_height="24dp"
            android:rotation="180"
            android:src="@drawable/ic_back"
            android:clickable="true"
            android:focusable="true"
            app:tint="@android:color/black" />
    </LinearLayout>

    <TableLayout
        android:id="@+id/tableCalendar"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="24dp"
        android:stretchColumns="*"
        app:layout_constraintTop_toBottomOf="@id/layoutMonth">

        <TableRow android:id="@+id/rowHeader">
            <TextView android:gravity="center" android:text="Sun" android:textColor="@android:color/darker_gray" />
            <TextView android:gravity="center" android:text="Mon" android:textColor="@android:color/darker_gray" />
            <TextView android:gravity="center" android:text="Tue" android:textColor="@android:color/darker_gray" />
            <TextView android:gravity="center" android:text="Wed" android:textColor="@android:color/darker_gray" />
            <TextView android:gravity="center" android:text="Thu" android:textColor="@android:color/darker_gray" />
            <TextView android:gravity="center" android:text="Fri" android:textColor="@android:color/darker_gray" />
            <TextView android:gravity="center" android:text="Sat" android:textColor="@android:color/darker_gray" />
        </TableRow>

        <!-- Dynamic rows will be added here -->

    </TableLayout>

    <Button
        android:id="@+id/btnConfirmDate"
        android:layout_width="match_parent"
        android:layout_height="56dp"
        android:layout_marginTop="32dp"
        android:backgroundTint="@android:color/black"
        android:text="Confirm"
        android:textAllCaps="false"
        android:textColor="@android:color/white"
        app:cornerRadius="28dp"
        app:layout_constraintTop_toBottomOf="@id/tableCalendar" />

</androidx.constraintlayout.widget.ConstraintLayout>